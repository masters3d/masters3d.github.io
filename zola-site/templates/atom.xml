<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <id>{{ get_url(path="atom.xml") }}</id>
    <title>{{ config.title }}{% if section %} - {{ section.title }}{% endif %}</title>
    {% if config.description %}
    <subtitle>{{ config.description }}</subtitle>
    {% endif %}
    <updated>{{ now() | date(format="%+") }}</updated>
    <link rel="alternate" type="text/html" href="{{ config.base_url }}{% if section %}{{ section.path }}{% endif %}"/>
    <link rel="self" type="application/atom+xml" href="{{ get_url(path="atom.xml") }}"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    {% if config.extra.github_username %}
    <author>
        <name>{{ config.extra.github_username }}</name>
        <uri>{{ config.base_url }}</uri>
    </author>
    {% endif %}

    {% if section %}
        {% if section.pages %}
            {% for page in section.pages %}
            <entry xml:lang="en">
                <title>{{ page.title }}</title>
                <id>{{ page.permalink }}</id>
                <link rel="alternate" type="text/html" href="{{ page.permalink }}"/>
                <published>{{ page.date | date(format="%+") }}</published>
                <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
                {% if page.description %}
                <summary>{{ page.description }}</summary>
                {% endif %}
                {% if page.content %}
                <content type="html">{{ page.content }}</content>
                {% endif %}
                {% if config.extra.github_username %}
                <author>
                    <name>{{ config.extra.github_username }}</name>
                    <uri>{{ config.base_url }}</uri>
                </author>
                {% endif %}
            </entry>
            {% endfor %}
        {% endif %}
    {% endif %}
</feed>